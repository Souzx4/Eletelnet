let codigoGerado = 0; //variavel global para armazenar o dogigo gerado

let emailAlvo = ""; // o email que estamos recuperando a senha

function enviarCodigo(){
    const email = document.getElementById("emailRecuperacao").value;

    
}